language: java
jdk:
  - openjdk11

script: mvn -f gson ProjectTest261P

install: mvn -f gson install -DskipTests=true

branches:
  except:
    - gh-pages

notifications:
  email: false

sudo: false

cache:
  directories:
    - $HOME/.m2